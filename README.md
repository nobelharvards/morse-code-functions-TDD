# morse-code-functions-TDD

Run `npm test` in the directory to run tests. Only has a limited subset of functions from [full morse code project](https://github.com/nobelharvards/morse-code-translator).
